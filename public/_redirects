# Cloudflare Pages SPA redirects
# Static HTML pages (etherscan, about, token, transparency) are served
# directly by Cloudflare Pages before this redirect rule applies.
# Cloudflare Pages automatically serves .html files for extensionless URLs.
/*    /index.html   200
